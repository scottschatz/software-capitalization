# ─── Database ───────────────────────────────────────────────────────
DATABASE_URL="postgresql://postgres@localhost:5432/software_capitalization?schema=public"

# ─── NextAuth ───────────────────────────────────────────────────────
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# ─── Auth Mode ──────────────────────────────────────────────────────
# For local dev without Azure AD:
DEV_AUTH_BYPASS="true"

# For production (Azure AD SSO):
# AZURE_AD_CLIENT_ID=""
# AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID="a473edd8-ba25-4f04-a0a8-e8ad25c19632"

# ─── AI (Anthropic) ────────────────────────────────────────────────
# Required for daily entry generation
ANTHROPIC_API_KEY=""

# ─── AI Model Configuration ────────────────────────────────────────
# Local model (LM Studio / Ollama / vLLM — OpenAI-compatible API)
# AI_LOCAL_URL="http://localhost:11434"
# AI_LOCAL_MODEL="openai/gpt-oss-20b"
# AI_LOCAL_ENABLED="true"
# Fallback model (Anthropic — used when local model is unavailable)
AI_FALLBACK_MODEL="claude-haiku-4-5-20251001"

# ─── Email (Optional) ──────────────────────────────────────────────
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="25"
# EMAIL_FROM="Cap Tracker <noreply@example.com>"
